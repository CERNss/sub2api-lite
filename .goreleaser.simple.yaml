# 简化版 GoReleaser 配置 - 仅发布容器镜像
version: 2

project_name: sub2api

before:
  hooks:
    - go mod tidy -C backend

builds:
  - id: sub2api
    dir: backend
    main: ./cmd/server
    binary: sub2api
    flags:
      - -tags=embed
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.Commit={{.Commit}}
      - -X main.Date={{.Date}}
      - -X main.BuildType=release

# 跳过 archives
archives: []

# 跳过 checksum
checksum:
  disable: true

changelog:
  disable: true

# 仅发布容器镜像
dockers:
  - id: dockerhub-amd64
    goos: linux
    goarch: amd64
    skip_push: '{{ if eq .Env.DOCKERHUB_REPOSITORY "skip" }}true{{ else }}false{{ end }}'
    image_templates:
      - "{{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}-amd64"
      - "{{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}"
      - "{{ .Env.DOCKERHUB_REPOSITORY }}:latest"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    extra_files:
      - deploy/docker-entrypoint.sh
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .Commit }}"
      - "--label=org.opencontainers.image.source=https://github.com/{{ .Env.GITHUB_REPO_OWNER }}/{{ .Env.GITHUB_REPO_NAME }}"

  - id: dockerhub-arm64
    goos: linux
    goarch: arm64
    skip_push: '{{ if eq .Env.DOCKERHUB_REPOSITORY "skip" }}true{{ else }}false{{ end }}'
    image_templates:
      - "{{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}-arm64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    extra_files:
      - deploy/docker-entrypoint.sh
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .Commit }}"
      - "--label=org.opencontainers.image.source=https://github.com/{{ .Env.GITHUB_REPO_OWNER }}/{{ .Env.GITHUB_REPO_NAME }}"

  - id: ghcr-amd64
    goos: linux
    goarch: amd64
    image_templates:
      - "{{ .Env.GHCR_REPOSITORY }}:{{ .Version }}-amd64"
      - "{{ .Env.GHCR_REPOSITORY }}:{{ .Version }}"
      - "{{ .Env.GHCR_REPOSITORY }}:latest"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    extra_files:
      - deploy/docker-entrypoint.sh
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .Commit }}"
      - "--label=org.opencontainers.image.source=https://github.com/{{ .Env.GITHUB_REPO_OWNER }}/{{ .Env.GITHUB_REPO_NAME }}"

  - id: ghcr-arm64
    goos: linux
    goarch: arm64
    image_templates:
      - "{{ .Env.GHCR_REPOSITORY }}:{{ .Version }}-arm64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    extra_files:
      - deploy/docker-entrypoint.sh
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .Commit }}"
      - "--label=org.opencontainers.image.source=https://github.com/{{ .Env.GITHUB_REPO_OWNER }}/{{ .Env.GITHUB_REPO_NAME }}"

docker_manifests:
  - name_template: "{{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}"
    skip_push: '{{ if eq .Env.DOCKERHUB_REPOSITORY "skip" }}true{{ else }}false{{ end }}'
    image_templates:
      - "{{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}-amd64"
      - "{{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}-arm64"

  - name_template: "{{ .Env.DOCKERHUB_REPOSITORY }}:latest"
    skip_push: '{{ if eq .Env.DOCKERHUB_REPOSITORY "skip" }}true{{ else }}false{{ end }}'
    image_templates:
      - "{{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}-amd64"
      - "{{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}-arm64"

  - name_template: "{{ .Env.GHCR_REPOSITORY }}:{{ .Version }}"
    image_templates:
      - "{{ .Env.GHCR_REPOSITORY }}:{{ .Version }}-amd64"
      - "{{ .Env.GHCR_REPOSITORY }}:{{ .Version }}-arm64"

  - name_template: "{{ .Env.GHCR_REPOSITORY }}:latest"
    image_templates:
      - "{{ .Env.GHCR_REPOSITORY }}:{{ .Version }}-amd64"
      - "{{ .Env.GHCR_REPOSITORY }}:{{ .Version }}-arm64"

release:
  github:
    owner: "{{ .Env.GITHUB_REPO_OWNER }}"
    name: "{{ .Env.GITHUB_REPO_NAME }}"
  draft: false
  prerelease: auto
  name_template: "Sub2API Lite {{.Version}} (Simple)"
  # 跳过上传二进制包
  skip_upload: true
  header: |
    > AI API Gateway Platform - 将 AI 订阅配额分发和管理
    > ⚡ Simple Release: 仅包含 amd64/arm64 容器镜像

    {{ .Env.TAG_MESSAGE }}

  footer: |
    ---

    ## 📥 Installation

    **Docker (amd64/arm64):**
    ```bash
    {{ if ne .Env.DOCKERHUB_REPOSITORY "skip" -}}
    # Docker Hub
    docker pull {{ .Env.DOCKERHUB_REPOSITORY }}:{{ .Version }}

    {{ end -}}
    # GitHub Container Registry
    docker pull {{ .Env.GHCR_REPOSITORY }}:{{ .Version }}
    ```

    ## 📚 Documentation

    - [GitHub Repository](https://github.com/{{ .Env.GITHUB_REPO_OWNER }}/{{ .Env.GITHUB_REPO_NAME }})
